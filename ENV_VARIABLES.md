# Переменные окружения для Easypanel

## Полный список переменных окружения

Скопируй эти переменные в настройки сервиса в Easypanel:

---

## 🔧 Общие переменные

```
NODE_ENV=production
```

---

## 🗄️ Backend переменные

### База данных
```
DATABASE_URL=postgresql://username:password@host:5432/database_name
```
**Пример:**
```
DATABASE_URL=postgresql://postgres:mypassword@db.example.com:5432/carthelp
```

### JWT секрет
```
JWT_SECRET=твой-очень-длинный-и-безопасный-секретный-ключ-минимум-32-символа
```
**Пример (сгенерируй свой уникальный):**
```
JWT_SECRET=a8f5f167f44f4964e6c998dee827110c8f5f167f44f4964e6c998dee827110c
```

### Порт Backend
```
PORT=3001
```

---

## 🌐 Frontend переменные

### URL Backend API
```
NEXT_PUBLIC_API_URL=
```
**Важно:** 
- **Оставь `NEXT_PUBLIC_API_URL` пустым** - Next.js автоматически проксирует запросы `/api/*` на бэкенд через rewrites в `next.config.js`
- Это работает потому что оба сервиса (фронтенд и бэкенд) запущены в одном контейнере
- Если по какой-то причине нужно использовать полный URL, установи: `NEXT_PUBLIC_API_URL=https://твой-домен.ru` (без `/api` в конце)
- **НЕ используй** `http://localhost:3001` - это не работает в браузере пользователя!

### NextAuth URL
```
NEXTAUTH_URL=https://твой-домен.ru
```
**Пример:**
```
NEXTAUTH_URL=https://carthelp.ru
```
или
```
NEXTAUTH_URL=https://carthelp.example.com
```

### NextAuth секрет
```
NEXTAUTH_SECRET=твой-очень-длинный-и-безопасный-секретный-ключ-для-nextauth
```
**Пример (сгенерируй свой уникальный, отличный от JWT_SECRET):**
```
NEXTAUTH_SECRET=b9g6g278g55g5075g7d9eef938ff221d9g6g278g55g5075g7d9eef938ff221d
```

### Порт Frontend
```
PORT=3000
```

---

## 📋 Полный список для копирования в Easypanel

Скопируй все эти переменные в настройки Environment Variables в Easypanel:

```
NODE_ENV=production
DATABASE_URL=postgresql://username:password@host:5432/database_name
JWT_SECRET=твой-очень-длинный-и-безопасный-секретный-ключ-минимум-32-символа
BACKEND_PORT=3001
NEXT_PUBLIC_API_URL=
NEXTAUTH_URL=https://твой-домен.ru
NEXTAUTH_SECRET=твой-очень-длинный-и-безопасный-секретный-ключ-для-nextauth
```

---

## ⚠️ Важные замечания

1. **DATABASE_URL** - замени на реальную строку подключения к PostgreSQL из Easypanel
2. **JWT_SECRET** - сгенерируй уникальный секретный ключ (минимум 32 символа). Можно использовать:
   ```bash
   openssl rand -base64 32
   ```
3. **NEXTAUTH_SECRET** - сгенерируй другой уникальный секретный ключ (отличный от JWT_SECRET):
   ```bash
   openssl rand -base64 32
   ```
4. **NEXTAUTH_URL** - должен быть полным URL твоего домена (с https://)
5. **NEXT_PUBLIC_API_URL** - оставь пустым! Next.js автоматически проксирует запросы через rewrites в `next.config.js`.
   Проксирование настроено автоматически - не нужно ничего делать в Easypanel.
   **НЕ используй** `http://localhost:3001` - это не работает в браузере!
6. **PORT** — в Easypanel платформенный `PORT` используется для фронтенда (Next.js) и может быть, например, `80`.  
   Backend теперь использует **только** `BACKEND_PORT=3001`, поэтому конфликта портов не будет.

---

## 🔐 Генерация секретных ключей

Если нужно сгенерировать безопасные секретные ключи, выполни в терминале:

```bash
# Для JWT_SECRET
openssl rand -base64 32

# Для NEXTAUTH_SECRET (другой ключ!)
openssl rand -base64 32
```

---

## ✅ Чеклист перед деплоем

- [ ] DATABASE_URL указан и доступен
- [ ] JWT_SECRET сгенерирован и установлен
- [ ] NEXTAUTH_SECRET сгенерирован и установлен (отличный от JWT_SECRET!)
- [ ] NEXTAUTH_URL указывает на твой домен с https://
- [ ] NEXT_PUBLIC_API_URL = (пустое значение для относительных путей или полный URL домена)
- [ ] Все переменные добавлены в Easypanel

